<h1>Products</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Category</th>
      <th>Weight type</th>
      <th>Quantity</th>
      <th>Price</th>
      <th>Sale price</th>
      <th>Company name</th>
      <th>Batch</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td class="product-pic"><%= image_tag product.product_pic.url %></td>
        <td><%= link_to product.name, product %></td>
        <td><%= product.category %></td>
        <td><%= number_to_human(product.weight_quantity, units: {unit: "г", thousand: "кг"}) %></td>
        <td><%#= product.weight_quantity %></td>
        <td><%= product.weight_type %></td>
        <td><%= product.quantity %></td>
        <td><%= product.price %></td>
        <td><%= product.sale_price %></td>
        <td><%= product.company_name %></td>
        <td><%= product.batch_id %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product', new_product_path %>
